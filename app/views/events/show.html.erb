<p id="notice"><%= notice %></p>

<h1><%= @event.name %></h1>

<p>
  <%= @event.description %>
</p>

<p>
  <ol>
    <% @event.timeslots.each do |timeslot| %>
      <li><%= timeslot.datetime %></li>
    <% end %>
  </ol>
</p>

<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>
